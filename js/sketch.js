function setup() {
    createCanvas(canvasWidth, canvasHeight);
    ellipseMode(CORNER);
}

function draw() {
    background(51);
    //drawScore();
    //drawTime();

    //drawBoard();
    //moveEnemy();
}